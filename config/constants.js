/** App-wide constants. Default timezone used when client does not send locale. */
class TimezoneConstants {
  static get DEFAULT_TIMEZONE() {
    return 'Asia/Kolkata';
  }
}

module.exports = { TimezoneConstants };
